---
const { headings = [] } = Astro.props;
const items = headings.filter((h) => h.depth >= 2 && h.depth <= 3);
---
{items.length > 0 && (
  <aside class="post-toc">
    <h4>目录</h4>
    <ul>
      {items.map((item) => (
        <li class={item.depth === 3 ? "toc-child" : ""}>
          <a href={`#${item.slug}`}>{item.text}</a>
        </li>
      ))}
    </ul>
  </aside>
)}
