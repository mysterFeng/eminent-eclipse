---
title: "19. Flutter 音频播放"
description: "基于 just_audio 的音频播放组件封装与常见用法。"
date: "2021-12-29"
category: "Flutter"
tags: ["组件"]
author: "myster"
summary: "从初始化、播放控制到进度监听，给出可复用的音频播放范式。"
featured: false
---

音频播放建议用 **just_audio**，稳定且功能完善。

## 0. 依赖

```yaml
dependencies:
  just_audio: ^0.9.34
```

## 1. 初始化与播放

```dart
final player = AudioPlayer();

Future<void> play(String url) async {
  await player.setUrl(url);
  player.play();
}
```

## 2. 暂停 / 停止

```dart
player.pause();
player.stop();
```

## 3. 进度监听

```dart
player.positionStream.listen((pos) {
  // pos.inSeconds
});
```

## 4. 播放状态监听

```dart
player.playerStateStream.listen((state) {
  if (state.processingState == ProcessingState.completed) {
    // 播放完成
  }
});
```

## 5. 释放资源

```dart
@override
void dispose() {
  player.dispose();
  super.dispose();
}
```

## 6. 常见坑点

- **未 dispose**：容易泄露
- **网络错误**：setUrl 可能抛异常，需 try/catch

## 7. 实践清单

- [ ] 初始化一次复用
- [ ] 监听进度与状态
- [ ] dispose 释放资源

---

