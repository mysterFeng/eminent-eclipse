---
title: "04. Flutter 文件选择"
description: "基于 file_picker 的文件选择与结果处理，包含权限与多选用法。"
date: "2021-04-12"
category: "Flutter"
tags: ["组件"]
author: "myster"
summary: "从文件选择、多选到结果处理，给出可复用范式。"
featured: false
---

文件选择常用于上传、导入。下面以 **file_picker** 给出完整实践。

## 0. 依赖

```yaml
dependencies:
  file_picker: ^5.5.0
```

## 1. 选择单个文件

```dart
final result = await FilePicker.platform.pickFiles();
if (result == null) return;
final file = result.files.single;
```

## 2. 多选文件

```dart
final result = await FilePicker.platform.pickFiles(allowMultiple: true);
if (result == null) return;
final files = result.files;
```

## 3. 只选特定类型

```dart
final result = await FilePicker.platform.pickFiles(
  type: FileType.custom,
  allowedExtensions: ['pdf', 'docx'],
);
```

## 4. 获取路径与字节

```dart
final path = file.path; // 适合上传
final bytes = file.bytes; // 适合直接处理
```

## 5. 常见坑点

- **iOS 路径为空**：需使用 `bytes`
- **权限未处理**：Android 需读存储权限

## 6. 实践清单

- [ ] 单选/多选支持
- [ ] 类型限制
- [ ] 处理路径/字节差异

---

