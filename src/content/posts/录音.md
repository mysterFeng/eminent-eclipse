---
title: "20. Flutter 录音"
description: "基于 record 的录音流程与权限处理。"
date: "2022-05-09"
category: "Flutter"
tags: ["组件"]
author: "myster"
summary: "从权限申请到录音文件输出，给出可复用录音范式。"
featured: false
---

录音场景建议使用 **record** 或自研插件。这里给出 `record` 的标准流程。

## 0. 依赖

```yaml
dependencies:
  record: ^5.0.1
```

## 1. 权限处理

```dart
final record = AudioRecorder();
final hasPermission = await record.hasPermission();
if (!hasPermission) return;
```

## 2. 开始录音

```dart
await record.start(
  const RecordConfig(encoder: AudioEncoder.aacLc),
  path: '/path/to/file.m4a',
);
```

## 3. 结束录音

```dart
final path = await record.stop();
```

## 4. 常见坑点

- **权限未授予**：录音失败
- **路径无效**：写入失败

---

